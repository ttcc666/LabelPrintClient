<ui:FluentWindow x:Class="LabelPrintClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:LabelPrintClient.Views"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="标签打印客户端" Height="760" Width="1180"
        WindowStartupLocation="CenterScreen"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <ui:TitleBar Title="标签打印客户端" Grid.Row="0" />
        
        <TabControl Grid.Row="1" Margin="8">
            <TabItem Header="标签打印中心">
                <views:PrintCenterView />
            </TabItem>
            <TabItem Header="模板维护">
                <views:TemplateManageView />
            </TabItem>
        </TabControl>
    </Grid>
</ui:FluentWindow>
