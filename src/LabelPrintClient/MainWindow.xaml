<Window x:Class="LabelPrintClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="标签打印客户端" Height="760" Width="1180"
        Icon="Assets/app-icon.ico"
        Background="{DynamicResource SecondaryRegionBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Height="40" Grid.Row="0">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <TextBlock Text="主题" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <Border CornerRadius="4" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Padding="2">
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="SystemThemeButton" Content="系统" Width="44" Height="28" Padding="0" Click="ThemeButton_Click" Tag="System"/>
                            <Button x:Name="LightThemeButton" Content="浅色" Width="44" Height="28" Padding="0" Margin="2,0,0,0" Click="ThemeButton_Click" Tag="Light"/>
                            <Button x:Name="DarkThemeButton" Content="深色" Width="44" Height="28" Padding="0" Margin="2,0,0,0" Click="ThemeButton_Click" Tag="Dark"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition>
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                            <Setter Property="Width" Value="56" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=MenuToggleButton}" Value="True">
                                    <Setter Property="Width" Value="200" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ColumnDefinition.Style>
                </ColumnDefinition>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
                <Grid Background="Transparent" MinWidth="48">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <ToggleButton x:Name="MenuToggleButton" Grid.Row="0" Height="48" MinWidth="48" IsChecked="True"
                              Background="Transparent" BorderThickness="0"
                              HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                              VerticalContentAlignment="Center" Padding="16,0,0,0"
                              ToolTip="收起 / 展开">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <iconPacks:PackIconMaterial Kind="Menu" Width="20" Height="20" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <TextBlock Text="收起 / 展开" VerticalAlignment="Center" Visibility="{Binding IsChecked, ElementName=MenuToggleButton, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </ToggleButton>

                <ListBox x:Name="RootNavigation" Grid.Row="1" SelectionChanged="RootNavigation_SelectionChanged"
                         Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBoxItem Tag="PrintCenter" Padding="16,10">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Printer" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="标签打印中心" VerticalAlignment="Center" Visibility="{Binding IsChecked, ElementName=MenuToggleButton, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem Tag="PrintHistory" Padding="16,10">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="History" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="打印记录" VerticalAlignment="Center" Visibility="{Binding IsChecked, ElementName=MenuToggleButton, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem Tag="TaskCenter" Padding="16,10">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="FormatListText" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="任务中心" VerticalAlignment="Center" Visibility="{Binding IsChecked, ElementName=MenuToggleButton, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem Tag="TemplateManage" Padding="16,10">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="PencilBoxOutline" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="模板维护" VerticalAlignment="Center" Visibility="{Binding IsChecked, ElementName=MenuToggleButton, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem Tag="Settings" Padding="16,10">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Cog" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="系统配置" VerticalAlignment="Center" Visibility="{Binding IsChecked, ElementName=MenuToggleButton, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>
            </Grid>
            </Border>

            <ContentControl x:Name="WorkspaceContent" Grid.Column="1" Margin="0" />
        </Grid>
    </Grid>
</Window>
