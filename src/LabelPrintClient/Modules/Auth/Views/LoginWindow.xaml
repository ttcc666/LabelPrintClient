<hc:Window x:Class="LabelPrintClient.Modules.Auth.Views.LoginWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:hc="https://handyorg.github.io/handycontrol"
           xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
           Title="系统登录"
           Width="420"
           Height="340"
           WindowStartupLocation="CenterScreen"
           ResizeMode="NoResize"
           Background="{DynamicResource SecondaryRegionBrush}"
           Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 顶部系统 Logo & 标题区 -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <Border Width="44" Height="44" CornerRadius="8" Background="#0F0066FF" Margin="0,0,12,0" VerticalAlignment="Center">
                <iconPacks:PackIconMaterial Kind="Printer" Width="22" Height="22" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="标签打印客户端"
                           Style="{StaticResource AppPageTitleStyle}"
                           FontSize="16"
                           Margin="0,0,0,4" />
                <TextBlock Text="请输入您的账号和密码以安全地使用系统"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="11" />
            </StackPanel>
        </DockPanel>

        <!-- 表单区域 -->
        <StackPanel Grid.Row="1">
            <StackPanel Margin="0,0,0,12">
                <TextBlock Text="用户名" Style="{StaticResource AppFormLabelStyle}" Margin="0,0,0,6" FontWeight="SemiBold" />
                <TextBox x:Name="UserNameBox" 
                         Padding="10,8" 
                         hc:InfoElement.Placeholder="请输入用户名" />
            </StackPanel>

            <StackPanel Margin="0,0,0,6">
                <TextBlock Text="密码" Style="{StaticResource AppFormLabelStyle}" Margin="0,0,0,6" FontWeight="SemiBold" />
                <PasswordBox x:Name="PasswordBox" 
                             Padding="10,8" 
                             KeyDown="PasswordBox_KeyDown" 
                             hc:InfoElement.Placeholder="请输入密码" />
            </StackPanel>

            <!-- 错误信息反馈 -->
            <TextBlock x:Name="ErrorText"
                       Foreground="{DynamicResource AppDangerBrush}"
                       FontSize="12"
                       Margin="0,4,0,0"
                       TextWrapping="Wrap" />
        </StackPanel>

        <!-- 底部按钮操作区 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Width="100" Style="{StaticResource AppDialogCancelButtonStyle}" Click="Cancel_Click" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="退出" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Width="100" Style="{StaticResource AppDialogPrimaryButtonStyle}" Click="Login_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Login" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="登录" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</hc:Window>
