<Window x:Class="LabelPrintClient.Modules.PrintCenter.Views.RowReprintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="{DynamicResource Reprint.Title}"
        Width="520"
        Height="490"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SecondaryRegionBrush}" Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Style="{StaticResource AppPageTitleStyle}" Text="{DynamicResource Reprint.PageTitle}" Margin="0,0,0,16" />

        <!-- 配置区域 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <!-- 产品肖像名片卡片 -->
            <Border BorderThickness="4,0,0,0" BorderBrush="{DynamicResource PrimaryBrush}" Background="#060066FF" CornerRadius="0,6,6,0" Padding="16,12" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock x:Name="ProductInfoText" Text="{DynamicResource Reprint.LoadingProduct}" TextWrapping="Wrap" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,6" />
                    <TextBlock x:Name="BatchInfoText" Text="{DynamicResource Reprint.BatchNoEmpty}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Border>

            <!-- 序列号标签选项：起止序列号 -->
            <StackPanel x:Name="SerialPanel" Margin="0,0,0,16" Visibility="Collapsed">
                <TextBlock Text="{DynamicResource Reprint.SerialRange}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="StartSerialBox" Grid.Column="0" IsEditable="False" hc:InfoElement.Placeholder="{DynamicResource Reprint.Start}" />
                    <TextBlock Grid.Column="1" Text="{DynamicResource Reprint.To}" VerticalAlignment="Center" Margin="8,0,8,0" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <ComboBox x:Name="EndSerialBox" Grid.Column="2" IsEditable="False" hc:InfoElement.Placeholder="{DynamicResource Reprint.End}" />
                </Grid>
            </StackPanel>

            <!-- 打印份数 -->
            <StackPanel x:Name="CopiesPanel" Margin="0,0,0,16">
                <TextBlock Text="{DynamicResource Reprint.Copies}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <TextBox x:Name="PrintCopiesBox" Text="1" hc:InfoElement.Placeholder="{DynamicResource Reprint.CopiesPlaceholder}" />
            </StackPanel>

            <!-- 打印机选择 -->
            <StackPanel Margin="0,0,0,8">
                <TextBlock Text="{DynamicResource Reprint.Printer}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ComboBox x:Name="PrinterNameBox" hc:InfoElement.Placeholder="{DynamicResource Reprint.PrinterPlaceholder}" />
            </StackPanel>
        </StackPanel>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Width="110" Style="{StaticResource AppPrimaryButtonStyle}" Click="Ok_Click" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Printer" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="{DynamicResource Reprint.StartPrint}" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Width="90" Style="{StaticResource AppDialogCancelButtonStyle}" Click="Cancel_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="{DynamicResource Common.Cancel}" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
