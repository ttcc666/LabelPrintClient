<Window x:Class="LabelPrintClient.Modules.Template.Views.TemplateEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="模板维护"
        Width="520"
        Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SecondaryRegionBrush}" Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock x:Name="TitleText" Grid.Row="0" Style="{StaticResource AppPageTitleStyle}" Text="新增模板" Margin="0,0,0,16" />

        <!-- 表单 -->
        <StackPanel Grid.Row="1">
            <StackPanel Margin="0,0,0,12">
                <TextBlock Text="模板名称" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <TextBox x:Name="NameBox" MaxLength="50" />
            </StackPanel>

            <StackPanel Margin="0,0,0,12">
                <TextBlock Text="存储介质" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ComboBox x:Name="StorageTypeBox" SelectedIndex="0">
                    <ComboBoxItem Content="本地物理文件 (LocalFile)" Tag="LocalFile" />
                    <ComboBoxItem Content="数据库存储 (Database)" Tag="Database" />
                </ComboBox>
            </StackPanel>

            <StackPanel Margin="0,0,0,12">
                <CheckBox x:Name="IsSerialNumberBox" Content="序列号控制" IsChecked="False" VerticalAlignment="Center" Margin="0,0,0,8" Checked="SerialRule_Changed" Unchecked="SerialRule_Changed" />
                <Grid IsEnabled="{Binding IsChecked, ElementName=IsSerialNumberBox}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="规则" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBox x:Name="SerialNumberPatternBox" Grid.Row="0" Grid.Column="1" Text="SN-{seq:0000}" MaxLength="200" TextChanged="SerialRule_Changed" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="重置周期" VerticalAlignment="Center" Margin="0,8,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <ComboBox x:Name="SerialResetPeriodBox" Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" SelectedIndex="0" SelectionChanged="SerialRule_Changed">
                        <ComboBoxItem Content="永不" Tag="Never" />
                        <ComboBoxItem Content="每天" Tag="Daily" />
                        <ComboBoxItem Content="每月" Tag="Monthly" />
                        <ComboBoxItem Content="每年" Tag="Yearly" />
                    </ComboBox>

                    <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,8,0,0">
                        <TextBlock Text="示例：SN-{yyyy}{MM}{dd}-{seq:0000}，变量：{yyyy} {yy} {MM} {dd} {HH} {mm} {seq:0000}"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="SerialPreviewText" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="预览：SN-0001" />
                    </StackPanel>
                </Grid>
            </StackPanel>

            <CheckBox x:Name="IsEnabledBox" Content="启用该模板" IsChecked="True" VerticalAlignment="Center" />
        </StackPanel>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource AppDialogPrimaryButtonStyle}" Click="Ok_Click" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Check" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="确定" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Style="{StaticResource AppDialogCancelButtonStyle}" Click="Cancel_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="取消" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
