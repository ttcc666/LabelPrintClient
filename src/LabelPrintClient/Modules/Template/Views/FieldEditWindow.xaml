<Window x:Class="LabelPrintClient.Modules.Template.Views.FieldEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="{DynamicResource Field.EditTitle}"
        Width="680"
        Height="660"
        MinWidth="620"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource SecondaryRegionBrush}" Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="20,20,0,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,20,16">
            <iconPacks:PackIconMaterial Kind="TextBoxPlusOutline" Width="24" Height="24" Margin="0,0,10,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
            <TextBlock x:Name="TitleText" Style="{StaticResource AppPageTitleStyle}" Text="{DynamicResource Field.AddTitle}" VerticalAlignment="Center" />
        </StackPanel>

        <!-- 表单 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,20,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <iconPacks:PackIconMaterial Kind="InformationOutline" Width="16" Height="16" Margin="0,0,6,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                    <TextBlock Text="{DynamicResource Field.BasicInfo}" Style="{StaticResource AppSectionTitleStyle}" VerticalAlignment="Center" />
                </StackPanel>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="{DynamicResource Field.Name}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="FieldNameBox" MaxLength="50" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="{DynamicResource Field.Code}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="FieldCodeBox" MaxLength="50" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="{DynamicResource Field.DataType}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <ComboBox x:Name="FieldTypeBox" SelectedIndex="0">
                            <ComboBoxItem Content="{DynamicResource Field.TypeString}" Tag="string" />
                            <ComboBoxItem Content="{DynamicResource Field.TypeInt}" Tag="int" />
                            <ComboBoxItem Content="{DynamicResource Field.TypeDecimal}" Tag="decimal" />
                            <ComboBoxItem Content="{DynamicResource Field.TypeDate}" Tag="date" />
                            <ComboBoxItem Content="{DynamicResource Field.TypeBool}" Tag="bool" />
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Bottom">
                        <CheckBox x:Name="IsRequiredBox" Content="{DynamicResource Field.RequiredField}" Height="36" />
                    </StackPanel>
                </Grid>

                <StackPanel Margin="0,16,0,12">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <iconPacks:PackIconMaterial Kind="ShieldCheckOutline" Width="16" Height="16" Margin="0,0,6,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource Template.ValidationRule}" Style="{StaticResource AppSectionTitleStyle}" VerticalAlignment="Center" />
                    </StackPanel>
                    <!-- 磨砂蓝说明面板 -->
                    <Border BorderThickness="1" BorderBrush="#300066FF" Background="#0A0066FF" Padding="12,10" CornerRadius="6">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <iconPacks:PackIconMaterial Kind="HelpCircleOutline" Width="14" Height="14" Margin="0,0,6,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                                <TextBlock Text="{DynamicResource Field.ValidationHelpTitle}" Foreground="{DynamicResource PrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="{DynamicResource Field.ValidationHelpLength}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="4,0,0,4" TextWrapping="Wrap" FontSize="11" />
                            <TextBlock Text="{DynamicResource Field.ValidationHelpRegex}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="4,0,0,4" TextWrapping="Wrap" FontSize="11" />
                            <TextBlock Text="{DynamicResource Field.ValidationHelpEnum}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="4,0,0,0" TextWrapping="Wrap" FontSize="11" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="{DynamicResource Field.MinLength}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="MinLengthBox" hc:InfoElement.Placeholder="{DynamicResource Field.Example1}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="{DynamicResource Field.MaxLength}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="MaxLengthBox" hc:InfoElement.Placeholder="{DynamicResource Field.Example50}" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="{DynamicResource Field.MinValue}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="MinValueBox" hc:InfoElement.Placeholder="{DynamicResource Field.Example0}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="{DynamicResource Field.MaxValue}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="MaxValueBox" hc:InfoElement.Placeholder="{DynamicResource Field.Example100}" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="{DynamicResource Field.RegexPattern}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="RegexPatternBox" MaxLength="500" hc:InfoElement.Placeholder="{DynamicResource Field.RegexPatternPlaceholder}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="{DynamicResource Field.RegexErrorMessage}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="RegexErrorMessageBox" MaxLength="200" hc:InfoElement.Placeholder="{DynamicResource Field.RegexErrorPlaceholder}" />
                    </StackPanel>
                </Grid>

                <StackPanel Margin="0,0,0,12">
                    <TextBlock Text="{DynamicResource Field.EnumOptions}" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBox x:Name="EnumOptionsBox" Height="72" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" hc:InfoElement.Placeholder="{DynamicResource Field.EnumPlaceholder}" />
                </StackPanel>

                <StackPanel Margin="0,8,0,12">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <iconPacks:PackIconMaterial Kind="TextBoxEditOutline" Width="16" Height="16" Margin="0,0,6,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource Field.Remark}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBox x:Name="RemarkBox" Height="72" TextWrapping="Wrap" AcceptsReturn="True" hc:InfoElement.Placeholder="{DynamicResource Field.RemarkPlaceholder}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,20,0">
            <Button Style="{StaticResource AppPrimaryButtonStyle}" Click="Ok_Click" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Check" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="{DynamicResource Common.Ok}" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Style="{StaticResource AppDialogCancelButtonStyle}" Click="Cancel_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="{DynamicResource Common.Cancel}" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
