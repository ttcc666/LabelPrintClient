<Window x:Class="LabelPrintClient.Modules.Template.Views.FieldEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="字段维护"
        Width="680"
        Height="660"
        MinWidth="620"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource SecondaryRegionBrush}" Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="20,20,0,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,20,16">
            <iconPacks:PackIconMaterial Kind="TextBoxPlusOutline" Width="24" Height="24" Margin="0,0,10,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
            <TextBlock x:Name="TitleText" Style="{StaticResource AppPageTitleStyle}" Text="新增字段" VerticalAlignment="Center" />
        </StackPanel>

        <!-- 表单 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,20,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <iconPacks:PackIconMaterial Kind="InformationOutline" Width="16" Height="16" Margin="0,0,6,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                    <TextBlock Text="基础信息" Style="{StaticResource AppSectionTitleStyle}" VerticalAlignment="Center" />
                </StackPanel>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="字段名称" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="FieldNameBox" MaxLength="50" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="字段编码" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="FieldCodeBox" MaxLength="50" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="数据类型" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <ComboBox x:Name="FieldTypeBox" SelectedIndex="0">
                            <ComboBoxItem Content="文本 (string)" Tag="string" />
                            <ComboBoxItem Content="整数 (int)" Tag="int" />
                            <ComboBoxItem Content="小数 (decimal)" Tag="decimal" />
                            <ComboBoxItem Content="日期 (date)" Tag="date" />
                            <ComboBoxItem Content="布尔 (bool)" Tag="bool" />
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Bottom">
                        <CheckBox x:Name="IsRequiredBox" Content="必填字段" Height="36" />
                    </StackPanel>
                </Grid>

                <StackPanel Margin="0,16,0,12">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <iconPacks:PackIconMaterial Kind="ShieldCheckOutline" Width="16" Height="16" Margin="0,0,6,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                        <TextBlock Text="校验规则" Style="{StaticResource AppSectionTitleStyle}" VerticalAlignment="Center" />
                    </StackPanel>
                    <!-- 磨砂蓝说明面板 -->
                    <Border BorderThickness="1" BorderBrush="#300066FF" Background="#0A0066FF" Padding="12,10" CornerRadius="6">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <iconPacks:PackIconMaterial Kind="HelpCircleOutline" Width="14" Height="14" Margin="0,0,6,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                                <TextBlock Text="校验规则使用说明" Foreground="{DynamicResource PrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="• 长度与数值：限制用户输入的文本字符数，或者限制数字的大小范围。" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="4,0,0,4" TextWrapping="Wrap" FontSize="11" />
                            <TextBlock Text="• 正则表达式：用于高级格式验证（如 ^1[3-9]\d{9}$ 限制为手机号）。" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="4,0,0,4" TextWrapping="Wrap" FontSize="11" />
                            <TextBlock Text="• 枚举值：按行输入各个选项，在录入时将自动以下拉框形式呈现。" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="4,0,0,0" TextWrapping="Wrap" FontSize="11" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="最小长度" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="MinLengthBox" hc:InfoElement.Placeholder="例如: 1" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="最大长度" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="MaxLengthBox" hc:InfoElement.Placeholder="例如: 50" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="最小数值" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="MinValueBox" hc:InfoElement.Placeholder="例如: 0" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="最大数值" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="MaxValueBox" hc:InfoElement.Placeholder="例如: 100" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="正则表达式" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="RegexPatternBox" MaxLength="500" hc:InfoElement.Placeholder="例如: ^[a-zA-Z0-9_]+$" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="正则错误提示" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="RegexErrorMessageBox" MaxLength="200" hc:InfoElement.Placeholder="例如: 只能包含字母、数字和下划线" />
                    </StackPanel>
                </Grid>

                <StackPanel Margin="0,0,0,12">
                    <TextBlock Text="枚举值" Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBox x:Name="EnumOptionsBox" Height="72" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" hc:InfoElement.Placeholder="每行输入一个选项，例如:&#x0a;选项A&#x0a;选项B" />
                </StackPanel>

                <StackPanel Margin="0,8,0,12">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <iconPacks:PackIconMaterial Kind="TextBoxEditOutline" Width="16" Height="16" Margin="0,0,6,0" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                        <TextBlock Text="备注说明" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBox x:Name="RemarkBox" Height="72" TextWrapping="Wrap" AcceptsReturn="True" hc:InfoElement.Placeholder="请输入字段备注..." />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,20,0">
            <Button Style="{StaticResource AppPrimaryButtonStyle}" Click="Ok_Click" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Check" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="确定" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Style="{StaticResource AppDialogCancelButtonStyle}" Click="Cancel_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                    <TextBlock Text="取消" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
